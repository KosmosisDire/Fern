---
Description: A ref type measures the handle, not the heap block
Expected: 8
---

type Program
{
    static fn Main -> i32
    {
        -- may depend on platform in the future
        return (i32)MemoryLayout<Core.Array<i32>>.Size()
    }
}
