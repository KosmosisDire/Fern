---
Description: CopyTo moves a run of elements between two arrays
Expected: 1230
---

type Program
{
    static fn Main -> i32
    {
        var a: i32[] = [1, 2, 3]
        var b: i32[] = [0, 0, 0, 0]
        a.CopyTo(b, 0, 0, 3)
        return b[0] * 1000 + b[1] * 100 + b[2] * 10 + b[3]
    }
}
