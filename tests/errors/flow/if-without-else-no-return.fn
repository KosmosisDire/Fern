---
Description: If with no else and no following return for the false branch
Expected: FN4001
---

type Program
{
    fn Bad(cond: bool) -> i32
    {
        if cond
        {
            return 1
        }
    }

    static fn Main -> i32
    {
        return Bad(true)
    }
}
