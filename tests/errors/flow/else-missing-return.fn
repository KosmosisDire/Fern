---
Description: Then-branch returns but else-branch does not
Expected: FN4001
---

type Program
{
    static fn Bad(cond: bool) -> i32
    {
        if cond
        {
            return 1
        }
        else
        {
            var x = 0
        }
    }

    static fn Main -> i32
    {
        return Bad(true)
    }
}
